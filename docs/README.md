# Documentation map

- `codex-surface-map.md` is the active platform map for this repository.
- `provenance.md` and `release-integrity.md` are inherited project history.
- `subagent-composition/` preserves the upstream investigation and design
  record. It contains historical Claude-era paths and claims; it is evidence,
  not active Codex installation or runtime guidance.

For current behavior, prefer the native manifest, root `AGENTS.md`, and each
skill's `SKILL.md` plus `agents/openai.yaml`.
